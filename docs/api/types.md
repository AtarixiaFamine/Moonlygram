# Types

The data models for Bot API objects. Each keeps the original response in a
`raw` dict, so fields that are not yet modelled stay reachable.

::: moonlygram.types
    options:
      show_root_heading: false
      filters: ["!^_"]
